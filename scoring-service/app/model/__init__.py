"""Judge-metrics ILLUSTRATIVE classifier package.

Separate from the shipped calibrated detector (app/scoring/acute.py). Nothing
here scores a real resident — it only demonstrates, on the committed curated
feature table, that the fall features are separable. See app/model/training.py.
"""
