# Scoring service — Python 3.11+ (tested on 3.13).
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0
pydantic>=2.6
numpy>=1.26
pandas>=2.1

# dev / test
pytest>=8.0
httpx>=0.27          # FastAPI TestClient
jsonschema>=4.0      # validate live responses vs contract.schema.json
