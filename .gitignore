# AI instructions — vault files, not source code
CLAUDE.md
CLAUDE.md.sha256

# AI volatile state — never commit
state.md
handoff.md
session-log.md
active/

# Vault records — source of truth is Obsidian, not git
intel.md
decisions.md

# Local navigation indexes — generated, never commit
_codebase-index.md
_manifest.md
_index.md

# Claude Code local config + vault-desk tooling (session/search helpers, not app code)
.claude/
scripts/
status.config.json
docs/adr/0001-filesystem-first-vault.md

# ── Mandatory vault baseline (retrofit 2026-07-01) ──
.env*
*.pem
*.key
.obsidian/
*.canvas
*.base
node_modules/
.next/
dist/
build/
__pycache__/
*.pyc
.pytest_cache/
.mkdocs-site/
*.log
.DS_Store
Thumbs.db

# real dataset files (large, licensed) - keep out of the vault repo,
# but COMMIT the provenance lock (source, sha256, size, date, citation)
scoring-service/data/**
!scoring-service/data/datasets.lock.json

# Tier-2 CURATED artifacts (2026-07-24 hosting design): tiny, derived from the
# raw datasets by scripts/curate.py, COMMITTED so a fresh clone / Render runs on
# REAL data with zero download. Re-include the dir first, then its contents.
!scoring-service/data/curated/
!scoring-service/data/curated/**

# data-prep tooling under scoring-service (app code, NOT the vault-desk scripts/
# on line 22) — committed for reproducibility of the curated artifacts above.
!scoring-service/scripts/
!scoring-service/scripts/**
