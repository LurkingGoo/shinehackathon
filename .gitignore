# AI instructions — vault file, not source code
CLAUDE.md

# AI volatile state — never commit
state.md
handoff.md
session-log.md
active/

# Vault records — source of truth is Obsidian, not git
intel.md
decisions.md

# Local navigation indexes — generated, never commit
_codebase-index.md
_manifest.md
_index.md

# Claude Code local config
.claude/

# ── Mandatory vault baseline (retrofit 2026-07-01) ──
.env*
*.pem
*.key
.obsidian/
*.canvas
*.base
node_modules/
.next/
dist/
build/
__pycache__/
*.pyc
.pytest_cache/
*.log
.DS_Store
Thumbs.db

# real dataset files (large, licensed) - keep out of the vault repo
scoring-service/data/
